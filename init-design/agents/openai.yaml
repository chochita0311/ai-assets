interface:
  display_name: "Init Design"
  short_description: "Turn a first screen into stable design rules"
  default_prompt: "Use $init-design to turn this first screen artifact into a durable design constitution."

policy:
  allow_implicit_invocation: true
