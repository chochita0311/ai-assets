interface:
  display_name: "GitHub PR Reviewer"
  short_description: "Review and safely maintain GitHub PR feedback"
  default_prompt: "Use $gh-review-pr to review a GitHub pull request from a frozen base/head snapshot, or safely amend or reply to its skill-owned findings when explicitly requested."
